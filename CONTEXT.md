# Domain Context

## group（调用组）

RPC 的路由单元：js 客户端以组名注册自己，调用方发起时点名组，服务端在组内任选一个在线 js 客户端执行动作。
- 包含：同组多客户端互为负载均衡；不同组的连接与调用完全隔离
- 不包含：跨组兜底——组内无在线客户端时调用直接以错误失败，不会借用其他组的客户端
- 关系：一次调用恰属于一个 group、恰携带一个 action

## action（被调动作）

js 客户端对外声明可执行的能力名；调用必须携带 action，接到调用的客户端若没有登记该动作的处理器，按协议回失败报文。
- 包含：同组各客户端可对同一 action 各自独立实现
- 不包含：服务端内置动作或默认动作——服务端不理解 action 的含义，只做路由
- 关系：action 在 group 内被解析执行；不同组的同名 action 互不相干

## clientId（客户端实例标识）

js 客户端连接会话的身份：每次页面加载生成一次，断线重连时沿用，服务端以它为注册键。
- 包含：同一客户端实例在重连、网络抖动场景下的身份延续
- 不包含：调用序号——那标识的是一次调用而非一条连接，两者不可混用
- 关系：group 由多个 clientId 构成；同一 clientId 重新注册表示旧连接被顶替
